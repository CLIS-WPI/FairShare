# Python version requirement: >=3.10 (Python 3.12 in Docker image)
# Note: Docker image (nvcr.io/nvidia/tensorflow:24.12-tf2-py3) includes Python 3.12

# Core dependencies
numpy>=1.21.0
scipy>=1.7.0
matplotlib>=3.4.0
pandas>=1.3.0
tqdm>=4.62.0

# Orbit propagation
sgp4>=2.21
skyfield>=1.42

# YAML configuration
pyyaml>=5.4.1

# Sionna for channel modeling (NVIDIA)
# Note: Sionna is installed separately in Dockerfile for version control
# sionna==1.2.1

# OpenNTN for NTN channel models
# Note: OpenNTN is installed from git in Dockerfile
# openntn>=0.1.0

# Testing
pytest>=7.0.0
pytest-cov>=3.0.0

# Jupyter for notebooks
jupyter>=1.0.0
ipykernel>=6.0.0

# Optional: for advanced visualization
seaborn>=0.11.0
plotly>=5.0.0

# RL frameworks
stable-baselines3>=2.0.0
gymnasium>=0.29.0
# Optional: for distributed training
# ray[rllib]>=2.8.0

# Deep learning for learned fairness metrics
torch>=2.0.0
torch-geometric>=2.3.0  # For graph-based fairness (optional)
scikit-learn>=1.3.0

# Interactive dashboards
dash>=2.14.0  # For interactive dashboards
streamlit>=1.28.0  # Alternative dashboard framework

# Data validation
scipy>=1.11.0  # Updated for validation utilities
